import { Text, View } from "react-native";
import React from "react";
import { HeaderContainer, Title } from "./Header.styles";

const Header = () => {
  return (
    <HeaderContainer>
      <Title>Find best recipes for cooking</Title>
    </HeaderContainer>
  );
};

export default Header;
